{% extends 'SyliusWebBundle:Frontend:layout.html.twig' %}

{% block content %}
<div class="jumbotron">
    <h1>{{ 'sylius.homepage.splash.headline'|trans }}</h1>
    <p class="lead">{{ 'sylius.homepage.splash.subheadline'|trans }}</p>
    <a class="btn btn-large btn-success" href="http://sylius.org">{{ 'sylius.homepage.splash.link'|trans }}</a>
</div>

{{ render(url('sylius_product_latest', {'limit': 9})) }}

{% endblock %}
