{% extends 'SyliusWebBundle:Frontend:layout.html.twig' %}

{% block content %}
<div class="page-header">
    <h1>{{ 'sylius.frontend.checkout.shipping.header'|trans|raw }}</h1>
</div>

{% include 'SyliusWebBundle:Frontend/Checkout:_progressBar.html.twig' %}

<form method="post" action="{{ path('sylius_checkout_forward', {'stepName': context.currentStep.name}) }}" class="form-horizontal" novalidate>

{{ form_errors(form.shippingMethod) }}

{{ form_label(form.shippingMethod) }}
<fieldset>
    {% for shippingMethodForm in form.shippingMethod %}
        {{ form_row(shippingMethodForm) }}
    {% endfor %}
</fieldset>

{{ form_widget(form._token) }}

<div class="form-actions">
    <a href="{{ path('sylius_checkout_display', {'stepName': context.previousStep.name}) }}" class="btn btn-large">{{ 'sylius.frontend.checkout.back'|trans }}</a>
    <input type="submit" class="btn btn-primary btn-large" value="{{ 'sylius.frontend.checkout.forward'|trans }}" />
</div>

</form>
{% endblock %}
