{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% block content %}
<div class="page-header">
    <h1>{{ 'sylius.backend.settings.taxation_header'|trans|raw }}</h1>
</div>

<form action="{{ path('sylius_backend_taxation_settings') }}" method="post" class="form-horizontal" novalidate>
    {{ form_errors(form) }}
    {{ form_row(form.default_tax_zone, {'attr': {'class': 'input-xlarge'}}) }}
    {{ form_rest(form) }}
    <div class="form-actions">
        <input type="submit" value="{{ 'sylius.backend.save_changes'|trans }}" class="btn btn-primary btn-large" />
        <a href="{{ path('sylius_backend_dashboard') }}" class="btn btn-large">{{ 'sylius.backend.cancel'|trans }}</a>
    </div>
</form>
{% endblock %}
