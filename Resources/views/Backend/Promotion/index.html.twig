{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% from 'SyliusWebBundle::macros.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/Promotion:macros.html.twig' import list %}

{% block content %}
<div class="page-header">
    <h1>{{ 'sylius.backend.promotion.index_header'|trans|raw }}</h1>
</div>

<div class="well">
    <a href="{{ path('sylius_backend_promotion_create') }}" class="btn btn-primary">
        <i class="icon-plus-sign"></i> {{ 'sylius.backend.promotion.create'|trans }}
    </a>
</div>

{{ pagination(promotions) }}
{{ list(promotions) }}
{{ pagination(promotions) }}
{% endblock %}
