{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusWebBundle::macros.html.twig' as macros %}
{% import 'SyliusWebBundle:Backend/TaxRate:macros.html.twig' as taxRateMacros %}

{% block content %}
<div class="page-header">
<h1>{{ 'sylius.backend.tax_rate.index_header'|trans|raw }}</h1>
</div>

<div class="well">
    <a href="{{ path('sylius_backend_tax_rate_create') }}" class="btn btn-primary">
        <i class="icon-plus-sign"></i> {{ 'sylius.backend.tax_rate.create'|trans }}
    </a>
    <a href="{{ path('sylius_backend_tax_category_index') }}" class="btn btn-success">
        <i class="icon-adjust"></i> {{ 'sylius.backend.tax_category.manage'|trans }}
    </a>
</div>

{{ macros.pagination(tax_rates) }}
{{ taxRateMacros.list(tax_rates) }}
{{ macros.pagination(tax_rates) }}

{% endblock %}
